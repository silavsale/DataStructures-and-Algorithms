/* eslint-disable semi */
const arr = [1, 3, 5, 7, 11, 16, 22];

const search = (array, target) => {
  for (let i = 0; i < array.length; i++) {
    const element = array[i];
  }

  return 1;
};

search(arr, 5);
